"""
Feedback package — deterministic worked solutions and misconception notes.

Entry points:
  solve(kind, ask, params) → Solved          (solve.py)
  get_notes(kind, ask, idx) → str            (misconceptions.py)
  compose_feedback(problem, chosen) → dict   (compose.py)
"""
